- name: Undeploy MariaDB stack
  hosts: opies_manager
  tasks:
    - name: Remove MariaDB stack
      shell: docker stack rm mariadb

- name: Delete files
  hosts: opies
  become: true
  tasks:
    - name: Delete data and files directories
      shell: rm -rf /home/orangepi/mariadb-primary-data/ /home/orangepi/mariadb-replica-data/ /home/orangepi/maxscale-data/ /home/orangepi/files/
